object HostUrl {

    object DEBUG {
        const val HOST = "<<DEBUG_URL>>"
    }

    object STAGE {
        const val HOST = "<<STAGE_URL>>"
    }

    object RELEASE {
        const val HOST = "<<RELEASE_URL>>"
    }
}